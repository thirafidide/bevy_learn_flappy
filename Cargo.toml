[package]
name = "bevy_flappy"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = { version = "0.8.0", features = ["dynamic"] }
bevy-inspector-egui = "0.12.1"
rand = "0.8.5"

[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3
